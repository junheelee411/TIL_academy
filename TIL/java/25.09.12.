25.09.12. FRI

- case 뒤의 값 : 리터럴

- ->(케이스라벨) : break; 포함5.09.12. FRI

- case 뒤의 값 : 리터럴

- ->(케이스라벨) : break; 포함

<do~while>

  int n=10;
	do // 조건을 나중에 비교 -> 적어도 한 번은 실행함
		{
			n++;
			System.out.println("while 안 : " + n); //11
		} while(n<10);

<무한루프 컴파일에러>
while(true)
		{
		}
		System.out.println("결과 : " + s);
  // 실행될 일 없으니 지우라고 뜸

<무한루프 비교>
  for(; ;) { }
  while(true { }
  // 동일하지만 while로 사용

for문 안에서 선언한 변수
-> 밖에서 호출하면 컴파일 에러 (유효 스코프 for문 안)

float 20억 != 20억50
유효자리수 문제

최대값 구할 때 변수 설정
- 가장 작은 값을 초기값으로
- 

  
