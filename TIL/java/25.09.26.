25.09.26. Fri

## 이클립스 저장
파일명 앞에 별 붙어있으면 저장 안 된 것

## throw new Exception(" ");
강제로 checked exception을 발생 시킴

## throws (메소드에만 붙일 수 있음) 
- 메세지를 호출하는 쪽에 예외 떠넘김(호출하는 쪽이 catch해야함)
- main()에서 예외 throws -> 예외 발생시 비정상적으로 프로그램 종료

## unchecked error 상위 -> runtime error

# Generics

## class Test6<T extends Number>
- 제한된 타입 파라미터
- Number를 상속 받은 클래스(Integer, Long, Double 등)만 가능
(Test6<String> t2 = new Test6<>(); 컴파일 에러)

## 제네릭 와일드 카드 <?> 
- 모든 클래스나 인터페이스가 가능
- 제네릭 타입에 의존하지 않는 메소드 등을 호출할 떄 사용

# Collection

## List
- ArrayList, LinkedList, Vector, Stack
- 순서 있음
- 인덱스로 삽입, 삭제 가능
- 중복 가능
- 객체 자체 저장 x, 주소 참조
- null 저장 가능 -> 해당 인덱스 객체 참조 x

## set
- HashSet, LinkedHashSet, TreeSet
## Map
