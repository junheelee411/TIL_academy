25.09.17. Wed

#   배열 복습

// int []x[] 2차원 배열

// 배열 범위 넘어나서 출력 -> outOfIndex (runtime error)

// 배열 선언 시 정수->0, String->null로 자동 초기화

// 배열 초기화 후 재 선언 시 전의 값 가비지컬렉터 대상이 됨

// 배열은 초기값 주면 [] 안에 숫자 쓰지 x

# 객체 지향 프로그래밍

// 절차지향 - 알고리즘에 따라 순차적인 처리 중시
- 처리 속도 빠름
- 재사용성, 유지보수 불리

// 객체지향
-데이터 중심
-확장 및 유지보수 용이

// 객체의 특징
-특성(속성) : 객체에 저장되어 있는 정보 
-행위 : 행동하거나 반응하는 방법
-정체성 : 다른 객체와 구별되는 식별 값

// 객체의 정의
-개발시간 : 레퍼런스(참조) 변수
-실행시간 : 클래스의 인스턴스(메모리를 할당받은 객체)

// 객체의 구성
- 속성 + 프로시저
- 필드(멤버변수) + 메소드5.09.17. Wed

#   배열 복습

// int []x[] 2차원 배열

// 배열 범위 넘어나서 출력 -> outOfIndex (runtime error)

// 배열 선언 시 정수->0, String->null로 자동 초기화

// 배열 초기화 후 재 선언 시 전의 값 가비지컬렉터 대상이 됨

// 배열은 초기값 주면 [] 안에 숫자 쓰지 x

# 객체 지향 프로그래밍

// 절차지향 - 알고리즘에 따라 순차적인 처리 중시
- 처리 속도 빠름
- 재사용성, 유지보수 불리

// 객체지향
-데이터 중심
-확장 및 유지보수 용이

// 객체의 특징
-특성(속성) : 객체에 저장되어 있는 정보 
-행위 : 행동하거나 반응하는 방법
-정체성 : 다른 객체와 구별되는 식별 값

// 객체의 정의
-개발시간 : 레퍼런스(참조) 변수
-실행시간 : 클래스의 인스턴스(메모리를 할당받은 객체)

// 객체의 구성
- 속성 + 프로시저(함수)
- 필드(멤버변수) + 메소드(멤버함수)

// 객체 = 클래스의 인스턴스(실제로 메모리에 할당된 것)

// 단일 책임의 원칙(SRP : The Single Responsibility Principle)
- 하나의 클래스 -> 하나의 책임

// 클래스 : 객체를 생성하는 틀(template)

# 클래스

//필드 - 초기화 하지 않으면 객체가 생성되는 시점에 객체 -> null, 숫자필드 -> 0으로 초기화

# 객체 사용

1. 객체 선언
-클래스명 객체명;

2. 객체 생성(메모리 할당)
-객체명 = new 클래스명();

클래스명 객체명 = new 클래스명();
(선언과 동시에 메모리 할당)

3. 객체 사용
-필드 : 객체명.필드
-메소드 : 객체명.메소드([인수])

# JVM은 main과 thread만 호출 가능

# 하나의 패키지 안에 중복된 클래스명 사용 불가

# 필드 접근 제어자
// private < 디폴트 < protected < public
	
private int a = 10; // 클래스 안에서만 접근 가능
int b = 20; // 동일한 패키지 안에서만 접근 가능
protected int c = 30; // 하위 클래스 및 동일한 패키지 안에서만 접근 가능
public int d =40; // 어디에서나 접근 가능

// import 해와도 클래스를 쓰기 위한 경로 지정일 뿐, 접근 권한 바꾸지 x

// public class 아닐 시 다른 패키지에서 import 불가

# 지역 변수(일반변수), 전역 변수(필드 변수), 필드 접근 제어자

// 메서드 안에 선언된 지역변수(로컬 변수)는 무조건 매서드 안에서만 사용 가능(필드 접근 제어자 의미 없음)
// 클래스 밑에 선언된 전역 변수(필드 변수) -> 필드 접근 제어자 사용


# 해쉬코드는 값을 기반으로 생성
- 값이 같으면 같을 수 있음 / 그러나 항상 같은 객체인 것은 아님

# 필드 변수 
-인스턴스변수(메모리 할당 필수/ 객체 생성 시마다 메모리 할당) - heap 가비지콜렉터
-클래스변수(클래스 로딩 될 때 한 번만 메모리 할당) : static - static 프로그램 종료시 소멸 

# 자바 최솽위 클래스 - object

# 하나의 파일 안에는 하나의 퍼블릭 클래스

# 생성자 안 만들면 자바 컴파일러가 자동으로 디폴트 생성자 만들어줌

# 지역 변수
-일반 지역 변수
-매개변수
-반복문 제어 변수
-조건문 내부 변수

변수
- 전역 변수(접근제어자 private < default < protected < public) - 인스턴스변수 / 클래스 변수
- 지역 변수



