25.09.10. WED

<연산 속도>
/, %가 제일 느림
(2의 보수 덧 여러번)

<홀짝 구분법>
이진수로 나타냈을 때 끝짜리 1이면 홀수/ 아니면 짝수

<비트 단위 연산 사용처>
암호화

<비트 부정>
양수 -> 양수+1의 음수값
음수 -> 음수-1의 양수값

<xor 스와핑>
int x=7, y=5;
		x=x^y;
		y=x^y;
		x=x^y;

<연산자 우선순위>
== > ||, &

<배정연산>
float f = 3.5f;
		c='A';
		c+=f;
		System.out.println(c);

  <비트단위 연산자>
  단축 연산 x

  <++>
  자바는 float, double도 ++ 가능
  		float 가=3.5f;
		++가;
		System.out.println(가);

  <유니코드>
  대문자 Z와 소문자 a 사이에 다른 문자 있음

  <boolean - 숫자자료형>
  while(1)
  이런 것도 안 됨
  아예 true!= 1
