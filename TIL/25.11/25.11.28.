25.11.28.

# 비동기 함수
- 작업을 기다리며 멈추지 않고, 다른 작업을 계속 진행할 수 있도록 해주는 함수
- 동기(synchronous)로 처리하면 작업이 끝날 때까지 코드 전체가 멈춰버림

// 3초 후에 한 번만 실행
setTimeout(`alert("환영합니다.");`, 3000);

// 2초 후 무한 반복
setInterval(`sub();`,2000);

// 타이머 종료
clearInterval(timer); 


1. 서버에서 html 다운
2. 다운된 정보를 메모리에 읽어와서 객체 생성
3. 화면 그림

document : 바디를 나타내는 자바스크립트 객체

String - new XXX

